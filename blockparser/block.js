var me = module.exports;
var utils = require( './blockparser/utils' )

me.parseRawBlock = function( blockBuf ) {
  // Can't use the same file pointer here
  // var blockHeader = utils.readNext( blockBuf, 80 );

}
